# [INF-04] Workflow legado — desativado.
# Os deploys reais estao em deploy-dev.yml, deploy-staging.yml e deploy-prod.yml,
# que usam ${{ secrets.PROD_PATH }}/${{ secrets.STAGING_PATH }}/${{ secrets.DEV_PATH }}
# para evitar paths hardcoded.
# Este arquivo e mantido apenas como historico e sera removido em seguida.
name: Deploy (DEPRECATED — ver deploy-prod.yml)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Este workflow foi substituido por deploy-prod.yml, deploy-staging.yml e deploy-dev.yml."
          exit 1
